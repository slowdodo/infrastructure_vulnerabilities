# ufw start

> ufw system enable
`` bash
sudo ufw enable
```

``` bash
sudo ufw 22/tcp
```

# check the rules

``` bash
sudo ufw enble
```


``` bash
sudo ufw status
```

``` bash
sudo ufw verbose
```

``` bash
sudo ufw status numbered
```
